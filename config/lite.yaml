run:
  profile: lite
  cadence_seconds_min: 45
  cadence_seconds_max: 120
  parallel_actions: 1
  restart_every_minutes: 90

repair:
  enabled: false
  max_repairs_per_run: 10

resources:
  sample_seconds: 60
