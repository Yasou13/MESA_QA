mesa:
  validation_mode: 0
  external_provider_enabled: false

run:
  profile: lite
  cadence_seconds_min: 45
  cadence_seconds_max: 120

repair:
  enabled: false
  max_repairs_per_run: 10

resources:
  sample_seconds: 60

